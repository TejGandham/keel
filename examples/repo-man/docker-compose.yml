services:
  app:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./repo_man:/app
      - ${REPOMAN_REPOS_PATH:-/mnt/agent-storage/vader/src}:/repos
      - ./.git-credentials:/root/.git-credentials:ro
    environment:
      - REPOMAN_PATH=/repos
      - REPOMAN_HOST_PATH=${REPOMAN_REPOS_PATH:-/mnt/agent-storage/vader/src}
      - PHX_HOST=localhost
      - GIT_TERMINAL_PROMPT=0
    stdin_open: true
    tty: true
