# F15 RepoServer PubSub
status: DONE
pipeline: backend
spec_ref: mvp-spec:5.2
needs: F12 (DONE)
acceptance: broadcasts {:repo_updated, status} on every state change

## Implementation Summary

- Added `broadcast/1` private function to `RepoServer` using `Phoenix.PubSub.broadcast/3`
- Topic: `"repos"`, message: `{:repo_updated, status}`
- PubSub name injectable via `:pubsub` key in repo_info (defaults to `RepoMan.PubSub`)
- `RepoMan.PubSub` confirmed in supervision tree (`application.ex`)
- Broadcast points: init, fetch start, fetch complete, pull start, pull complete, error, task crash
- 6 new Layer 3 (Mox) PubSub tests added to `repo_server_test.exs`
- All 54 tests + 5 properties pass, 0 failures
