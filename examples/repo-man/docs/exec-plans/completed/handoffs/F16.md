# F16 Supervisor + app wiring
status: DONE
pipeline: backend
spec_ref: mvp-spec:5.1
needs: F11-F15 (ALL DONE)
acceptance: DynamicSupervisor starts servers, REPOMAN_PATH crash if missing
designer_needed: YES (supervision tree, startup sequence)

## Completed 2026-03-14

### Files created/modified
- `lib/repo_man/repo_supervisor.ex` — NEW: DynamicSupervisor with start_repos/1, all_statuses/1, validate_repos_path!/1
- `lib/repo_man/application.ex` — MODIFIED: added Registry + RepoSupervisor to children, REPOMAN_PATH validation, post-boot start_repos call
- `config/test.exs` — MODIFIED: added start_repos_on_boot: false
- `test/repo_man/repo_supervisor_test.exs` — NEW: 10 tests covering start, discovery, all_statuses, path validation

### Children order
Telemetry → DNSCluster → PubSub → Registry (RepoMan.RepoRegistry) → TaskSupervisor → RepoSupervisor → Endpoint

### Key decisions
- start_repos/1 accepts keyword opts with application-level defaults for all dependencies (supervisor, registry, task_supervisor, pubsub, repos_path) — testable with isolated per-test infra
- all_statuses/1 queries Registry.select to find all servers, sorts by name
- validate_repos_path!/1 raises RuntimeError if path not a directory — fails app boot with clear message
- start_repos_on_boot config flag (default true) lets tests skip auto-start and manage repos explicitly
- Tests use stub (not expect) for repo? and git status calls since filesystem order is non-deterministic
