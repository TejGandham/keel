# F13 RepoServer fetch
status: DONE
pipeline: backend
spec_ref: mvp-spec:4.3.1
needs: F12 (DONE)
acceptance: async fetch, :fetching state, serialization guard (ignores if busy)
safety_auditor: PASSED

## Implementation Summary

### Changes
- `lib/repo_man/repo_server.ex` — Added `fetch/1` client API, `handle_cast(:fetch)` with serialization guard, async Task.Supervisor spawn, `handle_info` for task completion + crash, `status_attrs/2` helper
- `lib/repo_man/application.ex` — Added `{Task.Supervisor, name: RepoMan.TaskSupervisor}` to supervision tree
- `test/repo_man/repo_server_test.exs` — Added 5 fetch tests: :fetching state, serialization, idle after completion, status refresh, error on failure

### Tests (5 new, 12 total)
- fetch/1 sets operation to :fetching while in progress
- fetch/1 ignored when operation already in progress (serialization)
- After task completes, operation returns to :idle
- After task completes, status is refreshed (behind count updated)
- Task error sets operation to :error with last_error message

### Safety Audit
- Fetch delegates exclusively to @git_module.fetch/1 (no direct System.cmd)
- No --force, --rebase, checkout, reset, or stash in RepoServer
- Task.Supervisor.async_nolink isolates crashes from GenServer
- Operation serialization prevents concurrent ops per repo
