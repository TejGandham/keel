# F18 Clean card
status: DONE
pipeline: frontend
spec_ref: ui-design:4.1
needs: F17 (DONE)
acceptance: neutral border, no buttons, no pill, name+branch+ahead/behind

## Completed 2026-03-14

### What was built
- `repo_card/1` dispatcher — pattern-matches on `repo.severity`, routes `:clean` to `clean_card/1`, fallback for F19-F24
- `clean_card/1` — neutral gray left border (#333 dark / #d4d4d4 light), no buttons, no pill, shows name + branch (mono) + "clean" + ahead/behind + last fetch + terminal link
- `open_terminal_link/1` — reusable component, ghostty:// URI scheme, subtle ↗ link
- `format_time/1` — relative time helper (just now / Nm ago / Nh ago / Nd ago / never)
- `render/1` updated — replaced F17 placeholder with flow grid (`flex flex-wrap gap-2 items-start`) rendering cards
- Dark-first theme with light support via `dark:` Tailwind variants

### Files changed
- `lib/repo_man_web/live/dashboard_live.ex` — card components + helpers
- `test/repo_man_web/live/dashboard_live_test.exs` — 15 new tests (10 clean card, 5 format_time)

### Test results
- 20 dashboard tests pass (5 existing + 15 new)
- 88 total tests pass, 0 failures
